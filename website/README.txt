The website is an online interface for registering accounts and downloading the game. Should be located on the same host with the login server as the login server reads the same database

INSTRUCTIONS:
1. Create a mysql database with a table www_accounts, it should have the following fields: id (INT, primary key, auto_increment), username (varchar), password (varchar)
2. Update the fields in inc/settings.php
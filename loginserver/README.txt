loginAuthKey-variable should be the same as in the game server's globalvars.py file

If hosted on the same ip, the ports should be unique

GAME_VERSION variable should be updated whenever an update is rolled to the client side (and it should match with the client version's GAME_VERSION)

Update the database variables

Libaries needed to be included:
Crypto
twisted
bcrypt
pymysql